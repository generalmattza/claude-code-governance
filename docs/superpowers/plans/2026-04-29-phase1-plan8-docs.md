# Plan 8 - Documentation + v0.8.0-rc.0

> Status: 2026-04-29. Phase 1, Plan 8 of 10.

## Predecessor

`v0.7.0-beta.0` shipped Jamf Configuration Profile + tamper detection (`install-managed.sh`, `verify-managed.sh`, `docs/deployment/mdm-jamf.md`).

## Tasks

1. **Spec + plan docs.** Create this file plus `docs/superpowers/specs/2026-04-29-plan8-docs.md`.
2. **Auto-generators.** Create `scripts/gen-hook-docs.mjs` and `scripts/gen-coverage-matrix.mjs`. Wire `pnpm gen:hook-docs` and `pnpm gen:coverage-matrix`.
3. **Generate the 26 hook docs.** Run generator. Commit `docs/hooks/<name>.md` for all 26 hooks.
4. **`docs/known-bypasses.md`.** Document 8 known detection gaps with vector / detection status / recommended response.
5. **ADR-0005 + ADR-0006.** Rules-package decision (markdown templates over executable rules) and MDM deployment decision (Configuration Profile over PKG installer; per-OS path templates over auto-detect).
6. **Windows + Linux deployment guides.** Replace stub READMEs with substantive ~150 line guides each. Mark script artifacts as templates pending v1.1 / v1.2.
7. **README final polish + `docs/settings-reference.md`.** Move from "Plan 7 of 10" to "Plan 8 of 10". Add ToC, badges, honesty box. Document every settings.json key.
8. **CHANGELOG + final checks + tag.** Add `[0.8.0-rc.0] - 2026-04-29`. Run gates. Tag v0.8.0-rc.0. Push. Create GitHub release marked as prerelease.

## Acceptance

- 26 hook docs generated from manifests.
- `docs/coverage-matrix.md` regenerated by script.
- `docs/known-bypasses.md` is honest and useful.
- Two new ADRs explain decisions.
- Windows + Linux guides substantive (not stubs).
- README has ToC + badges + honesty box.
- `docs/settings-reference.md` covers every key.
- 313 tests still pass; coverage above 90 percent.
- v0.8.0-rc.0 tagged and pushed; GH release created (prerelease).
